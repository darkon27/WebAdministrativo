@model IList<TicketEstadistico>
@{ ViewBag.Title = "Index"; }
<section role="main" class="content-body">
    <h2>Bienvenidos al sistema de Gestión</h2>
    <section class="panel">
        <header class="panel-heading panel-heading-transparent">
            <div class="panel-actions">
                <a href="#" class="fa fa-caret-down"></a>
                <a href="#" class="fa fa-times"></a>
            </div>
            <h2 class="panel-title">Resumen de los tickets</h2>
        </header>
        <div class="panel-body">
            <div class="table-responsive">
                <table class="table table-striped mb-none">
                    <thead>
                        <tr>
                            <th>#</th>
                            <th>Tipos de Ticket</th>
                            <th>Total</th>
                            <th>Progreso Terminados</th>
                        </tr>
                    </thead>
                    <tbody>
                        @for (int i = 0; i < Model.Count; i++)
                        {
                            var item = Model[i];
            <tr>
                <td>@(i + 1)</td>
                <td>@item.DESTIPOTICKET</td>
                <td>
                    @*@{ var statusLabel = "";
                                        if (item.FINALIZADO > 0)
                                        {
                                            statusLabel = "Success";
                                        }
                                        else if (item.PENDIENTE > 0)
                                        {
                                            statusLabel = "Warning";
                                        }
                                        else if (item.ANULADO > 0)
                                        {
                                            statusLabel = "Danger";
                                        }
                                        else
                                        {
                                            statusLabel = "Info";
                                        } }*@
                    <span>@item.TOTAL_REGISTROS</span>
                </td>
                <td>
                    <div class="progress progress-sm progress-half-rounded m-none mt-xs light">
                        <div class="progress-bar progress-bar-primary" role="progressbar" aria-valuenow="@item.PORCENTAJE" aria-valuemin="0" aria-valuemax="100" style="width: @item.PORCENTAJE%;">
                            @item.PORCENTAJE%
                        </div>
                    </div>
                </td>
            </tr>}
                    </tbody>
                </table>
            </div>
        </div>
    </section>
</section>